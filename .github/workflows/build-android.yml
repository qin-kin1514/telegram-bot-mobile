name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # Set timeout to prevent hanging builds
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-8-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev
        sudo apt install -y build-essential libssl-dev libffi-dev python3-dev
        # Install additional dependencies for Android build
        sudo apt install -y ccache ant gradle
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        cmdline-tools-version: 11076708
        accept-android-sdk-licenses: true
        packages: "cmdline-tools;latest platform-tools platforms;android-31 build-tools;31.0.0 ndk;23.2.8568313 cmake;3.18.1"
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install buildozer cython
        # Install only buildozer dependencies, not all app requirements
        pip install kivy kivymd requests telethon pillow
    
    - name: Set Android environment variables
      run: |
        echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
        echo "ANDROID_SDK_ROOT=$ANDROID_HOME" >> $GITHUB_ENV
        echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/23.2.8568313" >> $GITHUB_ENV
        echo "ANDROID_NDK_ROOT=$ANDROID_HOME/ndk/23.2.8568313" >> $GITHUB_ENV
        echo "$ANDROID_HOME/platform-tools" >> $GITHUB_PATH
        echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH
        echo "$ANDROID_HOME/ndk/23.2.8568313" >> $GITHUB_PATH
    
    - name: Verify Android SDK and NDK installation
      run: |
        echo "Android SDK path: $ANDROID_HOME"
        echo "Android NDK path: $ANDROID_HOME/ndk/23.2.8568313"
        ls -la $ANDROID_HOME/
        ls -la $ANDROID_HOME/ndk/ || echo "NDK directory not found"
        ls -la $ANDROID_HOME/cmdline-tools/ || echo "cmdline-tools directory not found"
        which ant || echo "ANT not found in PATH"
        which sdkmanager || echo "sdkmanager not found in PATH"
        sdkmanager --version || echo "sdkmanager not working"
    
    - name: Cache Buildozer global directory
      uses: actions/cache@v4
      with:
        path: .buildozer_global
        key: buildozer-global-${{ hashFiles('buildozer.spec') }}
    
    - name: Cache Buildozer directory
      uses: actions/cache@v4
      with:
        path: .buildozer
        key: buildozer-${{ hashFiles('buildozer.spec') }}
    
    - name: Build APK
      run: |
        # Set environment variables for better build stability
        export GRADLE_OPTS="-Xmx2048m -Dorg.gradle.jvmargs=-Xmx2048m"
        export JAVA_OPTS="-Xmx2048m"
        # Build APK
        buildozer android debug
      env:
        GRADLE_OPTS: "-Xmx2048m -Dorg.gradle.jvmargs=-Xmx2048m"
        JAVA_OPTS: "-Xmx2048m"
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: bin/*.apk